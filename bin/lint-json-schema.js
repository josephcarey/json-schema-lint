#!/usr/bin/env node

// const defaultConfig = {
//   name: "default name",
// };

const main = async () => {
  console.log("running lint-json-schema...");
  // const rcConfig = require("rc")("lint-json-schema", defaultConfig);
  //   const [, , ...args] = process.argv;

  // const testRules = await require(rcConfig.rulePaths[0]);
  // testRules[0]();
  // console.log(testRules);
  // console.log('Hello world', args, JSON.stringify(rcConfig), rcConfig.name);

  // console.log(rcConfig);
};

main();
